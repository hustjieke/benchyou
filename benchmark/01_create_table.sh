 #!/bin/bash

 declare -a querys=(
 "CREATE TABLE `tdts_gantry_free_trans` (
  `id` varchar(64) NOT NULL COMMENT '计费交易编号',
  `province_type` varchar(64) NOT NULL COMMENT '省份',
  `msg_id` varchar(64) DEFAULT NULL COMMENT '通知消息号',
  `gantry_id` varchar(64) NOT NULL COMMENT '门架编号',
  `interval_id` varchar(64) NOT NULL COMMENT '收费单元编号',
  `computer_order` int(11) NOT NULL COMMENT '控制器序号',
  `hour_batch_no` varchar(64) NOT NULL COMMENT '小时批次号',
  `gantry_order_num` varchar(64) NOT NULL COMMENT '门架顺序号',
  `gantry_hex` varchar(64) NOT NULL COMMENT '门架Hex值',
  `gantry_hex_opposite` varchar(64) DEFAULT NULL COMMENT '对向门架Hex值',
  `trans_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '计费交易时间',
  `pay_fee` bigint(20) NOT NULL COMMENT '应收金额',
  `fee` bigint(20) NOT NULL COMMENT '交易金额',
  `discount_fee` bigint(20) NOT NULL COMMENT '优惠金额',
  `trans_fee` bigint(20) NOT NULL COMMENT '卡面扣费金额',
  `media_type` varchar(64) NOT NULL COMMENT '通行介质类型',
  `obu_sign` varchar(64) DEFAULT NULL COMMENT 'OBU单/双片标识',
  `toll_interval_id` varchar(64) DEFAULT NULL COMMENT '收费单元编号组合',
  `pay_fee_group` varchar(64) DEFAULT NULL COMMENT '应收金额组合',
  `fee_group` varchar(64) DEFAULT NULL COMMENT '交易金额组合',
  `discount_fee_group` varchar(64) DEFAULT NULL COMMENT '优惠金额组合',
  `vehicle_plate` varchar(64) DEFAULT NULL COMMENT '计费车辆车牌号码+颜色',
  `licence_code` varchar(64) NOT NULL COMMENT '识别车牌号',
  `licence_code2` varchar(64) NOT NULL COMMENT '识别车牌号',
  `licence_color` varchar(64) DEFAULT NULL COMMENT '识别牌号颜色',
  `vehicle_type` varchar(64) NOT NULL COMMENT '计费车型',
  `identify_vehicle_type` varchar(64) DEFAULT NULL COMMENT '识别的车型',
  `vehicle_class` varchar(64) NOT NULL COMMENT '车种',
  `tac` varchar(64) DEFAULT NULL COMMENT 'TAC',
  `trans_type` varchar(64) DEFAULT NULL COMMENT '交易类型标识',
  `terminal_no` varchar(64) DEFAULT NULL COMMENT '终端机编号',
  `terminal_transno` varchar(64) DEFAULT NULL COMMENT ' PSAM卡脱机交易序列号',
  `trans_no` varchar(64) DEFAULT NULL COMMENT 'IC卡交易序号',
  `service_type` varchar(64) DEFAULT NULL COMMENT '交易的服务类型',
  `algorithm_identifier` int(11) DEFAULT NULL COMMENT '算法标识',
  `key_version` varchar(64) DEFAULT NULL COMMENT '消费密钥版本号',
  `antenna_id` int(11) DEFAULT NULL COMMENT '天线ID编号',
  `rate_version` varchar(64) DEFAULT NULL COMMENT '计费模块和计费参数版本号',
  `consume_time` int(11) DEFAULT NULL COMMENT '交易耗时',
  `pass_state` int(11) DEFAULT NULL COMMENT '通行状态',
  `en_toll_lane_id` varchar(64) DEFAULT NULL COMMENT '入口车道编号',
  `en_toll_station_hex` varchar(64) DEFAULT NULL COMMENT '入口站hex字符串',
  `en_time` timestamp NOT NULL  COMMENT '入口交易发生的时间',
  `en_lane_type` varchar(64) DEFAULT NULL COMMENT '入口车道类型',
  `pass_id` varchar(64) NOT NULL COMMENT '通行标识ID',
  `media_num` varchar(64) NOT NULL COMMENT '通行介质编号',
  `last_gantry_hex` varchar(64) DEFAULT NULL COMMENT '上一个门架的hex编号',
  `last_gantry_time` timestamp NOT NULL  COMMENT '通过上一个门架的时间',
  `obu_mac` varchar(64) NOT NULL COMMENT 'OBU/CPC物理地址',
  `obu_issue_id` varchar(64) NOT NULL COMMENT 'OBU/CPC发行方标识',
  `obu_sn` varchar(64) NOT NULL COMMENT 'OBU/CPC序号编码',
  `obu_version` int(11) DEFAULT NULL COMMENT 'OBU/CPC版本号',
  `obu_start_date` int(11) DEFAULT NULL COMMENT 'OBU/CPC起始日期',
  `obu_end_date` int(11) DEFAULT NULL COMMENT 'OBU/CPC截止日期',
  `obu_electrical` int(11) DEFAULT NULL COMMENT 'OBU/CPC卡电量百分比',
  `obu_state` varchar(64) DEFAULT NULL COMMENT 'OBU/CPC状态',
  `obu_vehicle_plate` varchar(64) DEFAULT NULL COMMENT 'OBU内车牌号码',
  `obu_licence_code` varchar(64) DEFAULT NULL COMMENT 'OBU内车牌号',
  `obu_licence_color` varchar(64) DEFAULT NULL COMMENT 'OBU内牌号颜色',
  `obu_vehicle_type` varchar(64) DEFAULT NULL COMMENT 'OBU内车型',
  `vehicle_user_type` int(11) DEFAULT NULL COMMENT '车辆用户类型',
  `vehicle_seat` int(11) DEFAULT NULL COMMENT '车辆座位数',
  `axle_count` int(11) DEFAULT NULL COMMENT '车轴数',
  `total_weight` int(11) DEFAULT NULL COMMENT '车货总重',
  `vehicle_length` int(11) DEFAULT NULL COMMENT '车辆长',
  `vehicle_width` int(11) DEFAULT NULL COMMENT '车辆宽长',
  `vehicle_hight` int(11) DEFAULT NULL COMMENT '车辆高',
  `cpu_net_id` varchar(64) DEFAULT NULL COMMENT 'CPU卡片网络编号',
  `cpu_issue_id` varchar(64) DEFAULT NULL COMMENT 'CPU卡片发行方标识',
  `cpu_vehicle_plate` varchar(64) DEFAULT NULL COMMENT 'CPU内车牌号码',
  `cpu_licence_code` varchar(64) DEFAULT NULL COMMENT 'CPU内车牌号',
  `cpu_licence_color` varchar(64) DEFAULT NULL COMMENT 'CPU内车牌颜色',
  `cpu_vehicle_type` varchar(64) DEFAULT NULL COMMENT 'CPU内车型',
  `cpu_start_date` int(11) DEFAULT NULL COMMENT 'CPU起始日期',
  `cpu_end_date` int(11) DEFAULT NULL COMMENT 'CPU截止日期',
  `cpu_version` int(11) DEFAULT NULL COMMENT 'CPU用户卡版本号',
  `cpu_card_type` varchar(64) DEFAULT NULL COMMENT 'CPU卡类型',
  `cpu_card_id` varchar(64) DEFAULT NULL COMMENT 'CPU卡编号',
  `balance_before` bigint(20) DEFAULT NULL COMMENT '交易前余额',
  `balance_after` bigint(20) DEFAULT NULL COMMENT '交易后余额',
  `gantry_pass_count` int(11) DEFAULT NULL COMMENT 'CPC卡过站信息数量',
  `gantry_pass_info` varchar(64) DEFAULT NULL COMMENT 'CPC卡的过站信息',
  `fee_prov_info` varchar(64) DEFAULT NULL COMMENT 'CPC卡省内计费信息',
  `fee_sum_local_before` bigint(20) DEFAULT NULL COMMENT 'CPC卡本省费用累计前金额',
  `fee_sum_local_after` bigint(20) DEFAULT NULL COMMENT 'CPC卡本省费用累计后金额',
  `fee_calc_result` int(11) DEFAULT NULL COMMENT '计费接口特情值',
  `fee_info1` varchar(64) DEFAULT NULL COMMENT '计费信息1',
  `fee_info2` varchar(64) DEFAULT NULL COMMENT '计费信息2',
  `fee_info3` varchar(64) DEFAULT NULL COMMENT '计费信息3',
  `holiday_state` int(11) DEFAULT NULL COMMENT '节假日状态',
  `trade_result` int(11) NOT NULL COMMENT '交易结果',
  `special_type` varchar(64) DEFAULT NULL COMMENT '特情类型',
  `verify_code` varchar(64) NOT NULL COMMENT '校验码',
  `interrupt_signal` int(11) DEFAULT NULL COMMENT '干扰信号',
  `vehicle_pic_id` varchar(64) DEFAULT NULL COMMENT '车牌识别流水号',
  `vehicle_tail_pic_id` varchar(64) DEFAULT NULL COMMENT '车牌识别流水号（车尾）',
  `match_status` int(11) NOT NULL COMMENT '匹配状态',
  `valid_status` int(11) NOT NULL COMMENT '去重状态',
  `deal_status` int(11) NOT NULL COMMENT '处理状态',
  `related_trade_id` varchar(64) DEFAULT NULL COMMENT '交易成功关联流水号',
  `all_related_trade_id` varchar(64) DEFAULT NULL COMMENT '全部关联流水号',
  `station_db_time` timestamp NOT NULL  COMMENT '门架后台入库时间',
  `station_deal_time` timestamp NOT NULL  COMMENT '门架后台处理时间',
  `station_valid_time` timestamp NOT NULL  COMMENT '门架后台去重时间',
  `station_match_time` timestamp NOT NULL  COMMENT '门架后台匹配时间',
  `vehicle_sign` varchar(64) NOT NULL COMMENT '车辆识别标识',
  `receive_time` timestamp NOT NULL  COMMENT '接收时间',
  `insert_time` timestamp NOT NULL  COMMENT '入库时间',
  `sub_partition` bigint(20) NOT NULL,
  `charges_special_type` varchar(64) DEFAULT NULL,
  `special_type1` varchar(64) DEFAULT NULL,
  `auth_id` varchar(64) DEFAULT NULL COMMENT '鉴权',
  `fee_calc_special` int(11) DEFAULT NULL COMMENT '计费接口特情值',
  `last_gantry_hex_fee` varchar(64) DEFAULT NULL COMMENT '计费文件中上一个门架的hex编号',
  `last_gantry_hex_pass` varchar(64) DEFAULT NULL COMMENT '过站信息中上一个门架的hex编号',
  `rate_compute` int(11) DEFAULT NULL COMMENT '计费处理模式',
  `rate_fit_count` int(11) DEFAULT NULL COMMENT '计费拟合点数',
  `obu_fee_sum_before` bigint(20) DEFAULT NULL COMMENT '本次交易前标签累计金额',
  `obu_fee_sum_after` bigint(20) DEFAULT NULL COMMENT '本次交易后标签累计金额',
  `obu_prov_fee_sum_before` bigint(20) DEFAULT NULL COMMENT '本次交易前标签累计金额 （省内）',
  `obu_prov_fee_sum_after` bigint(20) DEFAULT NULL COMMENT '本次交易后标签累计金额 （省内）',
  `card_fee_sum_before` bigint(20) DEFAULT NULL COMMENT '本次交易前卡片累计金额',
  `card_fee_sum_after` bigint(20) DEFAULT NULL COMMENT '本次交易后卡片累计金额',
  `no_card_times_before` int(11) DEFAULT NULL COMMENT '本次交易前累计无卡次数',
  `no_card_times_after` int(11) DEFAULT NULL COMMENT '本次交易后累计无卡次数',
  `province_num_before` int(11) DEFAULT NULL COMMENT '本次交易前累计省份数量',
  `province_num_after` int(11) DEFAULT NULL COMMENT '本次交易后累计省份数量',
  `obu_total_trade_succ_num_before` int(11) DEFAULT NULL COMMENT '本次交易前标签累计写入成功总量',
  `obu_total_trade_succ_num_after` int(11) DEFAULT NULL COMMENT '本次交易后标签累计写入成功总量',
  `obu_prov_trade_succ_num_before` int(11) DEFAULT NULL COMMENT '省内本次交易前标签累计写入成功数量',
  `obu_prov_trade_succ_num_after` int(11) DEFAULT NULL COMMENT '省内本次交易后标签累计写入成功数量',
  `obu_trade_result` int(11) DEFAULT NULL COMMENT '标签交易结果',
  `obu_verify_code` varchar(64) DEFAULT NULL COMMENT '标签交易流水验证码',
  `trade_type` int(11) DEFAULT NULL COMMENT '交易类型',
  `obu_info_type_read` int(11) DEFAULT NULL COMMENT '读取标签入口信息类型',
  `obu_info_type_write` int(11) DEFAULT NULL COMMENT '本次写入标签入口信息类型',
  `obu_pass_state` int(11) DEFAULT NULL COMMENT '标签入口状态',
  `fee_vehicle_type` varchar(64) DEFAULT NULL COMMENT '计费车型',
  `obu_last_gantry_hex` varchar(64) DEFAULT NULL COMMENT 'OBU中上一个门架HEX',
  `obu_last_gantry_time` timestamp NOT NULL  COMMENT 'OBU中通过上一个门架时间',
  `transaction_type` int(11) DEFAULT NULL COMMENT '交易流水类别',
  `gantry_type` varchar(64) DEFAULT NULL COMMENT '门架类型',
  `toll_interval_sign` varchar(64) DEFAULT NULL COMMENT '收费单元处理标识',
  `obu_pay_fee_sum_before` bigint(20) NOT NULL COMMENT '本次交易前标签累计应收金额',
  `obu_pay_fee_sum_after` bigint(20) NOT NULL COMMENT '本次交易后标签累计应收金额',
  `obu_discount_fee_sum_before` bigint(20) NOT NULL COMMENT '本次交易前标签累计优惠金额',
  `obu_discount_fee_sum_after` bigint(20) NOT NULL COMMENT '本次交易后标签累计优惠金额',
  `trade_read_ciphertext` varchar(64) DEFAULT NULL COMMENT '本次交易读取的EF04加密摘要',
  `read_ciphertext_verify` int(11) NOT NULL COMMENT '本次交易读取的EF04加密摘要验证结果',
  `trade_write_ciphertext` varchar(64) DEFAULT NULL COMMENT '本次交易生成的EF04加密摘要',
  `fee_mileage` int(11) DEFAULT NULL COMMENT '计费里程数',
  `obu_mileage_before` int(11) NOT NULL COMMENT '本次交易前标签累计里程',
  `obu_mileage_after` int(11) NOT NULL COMMENT '本次交易后标签累计里程',
  `prov_min_fee` bigint(20) DEFAULT NULL COMMENT '本省累计通行金额参考',
  `fee_spare1` int(11) DEFAULT NULL COMMENT '计费协议保留字段1',
  `fee_spare2` int(11) DEFAULT NULL COMMENT '计费协议保留字段2',
  `fee_spare3` varchar(64) DEFAULT NULL COMMENT '计费协议保留字段3',
  `fee_prov_begin_hex` varchar(64) DEFAULT NULL COMMENT '本省计费起点',
  `obu_prov_pay_fee_sum_before` bigint(20) NOT NULL COMMENT '本次交易前标签累计应收金额（省内）',
  `obu_prov_pay_fee_sum_after` bigint(20) NOT NULL COMMENT '本次交易后标签累计应收金额（省内）',
  `path_fit_flag` int(11) NOT NULL COMMENT '本次计费拟合结果标识',
  `fee_calc_specials` varchar(64) NOT NULL COMMENT '计费特情值组合',
  `pay_fee_prov_sum_local` bigint(20) NOT NULL COMMENT '本省累计金额取整',
  `pcrsu_version` int(11) NOT NULL COMMENT 'PC-RSU接口协议版本号',
  `gantry_pass_info_after` varchar(64) DEFAULT NULL COMMENT '本次写入的CPC卡EF02文件中过站信息',
  `update_result` int(11) NOT NULL COMMENT '更新过站信息结果',
  `cpc_fee_trade_result` int(11) NOT NULL COMMENT 'PC卡计费信息文件写入结果',
  `fee_prov_ef04` varchar(64) NOT NULL COMMENT '本次写入到CPC EF04中本省计费信息',
  `fit_prov_flag` int(11) NOT NULL COMMENT 'CPC卡是否进行全省路径拟合的标识',
  `gantry_pass_count_before` int(11) NOT NULL COMMENT 'CPC卡EF02文件中过站信息中门架数量',
  `fee_prov_begin_hex_fit` varchar(64) DEFAULT NULL COMMENT '拟合后的本省省界入口ETC门架HEX码',
  `fee_prov_begin_time_fit` timestamp NOT NULL  COMMENT '拟合后的本省省界入口ETC门架的通行时间',
  `is_fix_data` int(11) DEFAULT NULL COMMENT '是否修正过',
  `rsu_manu_id` varchar(64) DEFAULT NULL COMMENT '路侧单元厂商代码',
  `fee_data_version` varchar(64) DEFAULT NULL COMMENT '计费协议数据结构版本号',
  `vehicle_class_num` int(11) DEFAULT NULL,
  PRIMARY KEY (`id`,`licence_code`,`sub_partition`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收费门架计费收费信息';"
)


# mysql -e "..."
for q in "${querys[@]}"
do
    #r="$(clickhouse-client --compression=0 --database=benchmark --time --query="$q" 2>&1| tail -n 1)";
    #echo '|' $q '|' $r's' '|'
    echo $q
done
